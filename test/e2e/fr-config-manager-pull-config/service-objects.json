{
  "alpha_user": [
    {
      "searchField": "sn",
      "searchValue": "User",
      "fields": ["userName", "givenName", "sn", "mail", "authzRoles"],
      "overrides": {
	      "userName": "newUser",
	      "givenName": "Test",
	        "password": "${SERVICE_ACCOUNT_JOURNEY_PASSWORD}"

      }
    }
  ],
  "alpha_role": [
    {
      "searchField": "name",
      "searchValue": "Test Role",
      "fields": ["name", "description"],
      "overrides": {}
    }
  ],
  "bravo_user": [
    {
      "searchField": "userName",
      "searchValue": "bravoTestUser",
      "fields": ["userName", "givenName", "sn", "mail", "authzRoles"],
      "overrides": {
        "givenName": "Changed"
      }
    }
  ]
}
